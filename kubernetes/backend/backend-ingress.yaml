apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: personal-diary-api-ingress
  namespace: personal-diary
  labels:
    app.kubernetes.io/name: personal-diary-api
    app.kubernetes.io/component: ingress
    app.kubernetes.io/part-of: personal-diary
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-production
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
spec:
  ingressClassName: traefik
  tls:
  - hosts:
    - api.diary.xmojo.net
    secretName: api-diary-xmojo-net-tls
  rules:
  - host: api.diary.xmojo.net
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: personal-diary-api-service
            port:
              number: 8000